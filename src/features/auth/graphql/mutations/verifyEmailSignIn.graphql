mutation VerifyEmailSignIn($input: VerifyEmailSignInInput!) {
  verifyEmailSignIn(input: $input) {
    session {
      ...AuthSessionFields
    }
    user {
      ...AuthUserFields
    }
    error
  }
}
